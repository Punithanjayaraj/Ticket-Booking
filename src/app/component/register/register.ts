export interface Register {
    name: string;
    email: string;
    password: string;
    confrimpassword: string;
    contact:number;
}

export interface UserInterface{
    id:any,
    email:any,
    name: any,
    contact:any
    role:any;
}